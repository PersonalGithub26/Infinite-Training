<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:ui="http://java.sun.com/jsf/facelets"
      xmlns:f="http://java.sun.com/jsf/core"
      xmlns:h="http://java.sun.com/jsf/html">

<h:form>
		<ui:include src="/CustomerMenu.xhtml" />
				<center>
         	<h:selectOneMenu value="#{rDao.localCode}" onchange="submit()"
	valueChangeListener="#{rDao.restaurantLocaleCodeChanged}">
    <f:selectItems value="#{rDao.showRestaurantNames()}"/>
    </h:selectOneMenu>
         	<!-- <h:selectOneMenu value="#{vDao.localCode}" onchange="submit()"
	valueChangeListener="#{vDao.vendorLocaleCodeChanged}">
    <f:selectItems value="#{vDao.showVendorNames()}"/>
</h:selectOneMenu>
Selected vendor is : 
<h:outputText value="#{vDao.vendorId}"/> -->
&nbsp;&nbsp;&nbsp;
Selected value is : 
<h:outputText value="#{rDao.restaurantId}"/>
         <h:dataTable value="#{mDao.showMenuByRestaurant(rDao.restaurantId)}" 
        		var="e" border="3">
        	    <h:column>
                    <f:facet name="header">Menu Id</f:facet>
                    #{e.menuId}
                </h:column>
                  <h:column>
                    <f:facet name="header">Item Name</f:facet>
                    #{e.menuItem}
                </h:column>
                  <h:column>
                    <f:facet name="header">Item Price</f:facet>
                    #{e.menuPrice}
                </h:column>
                  <h:column>
                    <f:facet name="header">Calories</f:facet>
                    #{e.calories}
                </h:column>
                  <h:column>
                    <f:facet name="header">Speciality</f:facet>
                  #{e.menuSpeciality}"
                </h:column>
        </h:dataTable>
            </center>
            
            Projection projection = Projections.enum("walletSource", WalletSource.class);
</h:form>
</html>